# 案例：Python 冒泡排序算法探究与实现 (Example Case)

本案例完整展示高中信息科技学科在粤教版课程背景下，使用 Teaching Skills Framework 生成教学设计与课堂学习任务单的真实输入与输出对照。

---

## 1. 教学背景与学情研判 (Context)

### 课程地位
排序算法是高中信息科技“算法与程序设计”模块的里程碑内容，承载着双重循环嵌套控制逻辑与算法时空复杂度认知的核心任务。

### 典型认知障碍
- 难以直观理解双层嵌套循环中外层循环（趟数）与内层循环（每趟比较次数）的数学递减关系。
- 容易混淆“两数交换”的中间变量机制（引入第三杯水比喻）。

---

## 2. 技能输入参数 (Input Parameters)

```yaml
topic: Python 冒泡排序算法探究与实现
subject: information-technology
grade_level: 高中一年级
duration: 45 分钟
target_skills:
  - it.algorithm
  - it.programming
  - core.activity-design
textbook_edition: 粤教版高中信息技术必修1《数据与计算》
prior_knowledge: 学生已掌握 Python 列表索引、单层 for 循环、两数交换变量操作
lab_environment: 具备网络与 Python 3.10 环境的计算机机房
```

---

## 3. 生成产物一：《Python 冒泡排序算法探究与实现》教学设计

### 一、课程基本信息
- **学科**：信息科技 (普通高中必修1《数据与计算》)
- **学段/年级**：高一年级
- **课时安排**：45 分钟 (1 课时)
- **教材版本**：普通高中教科书粤教版

### 二、教材与学情分析
- **教材分析**：本课位于第 4 章第 2 节，引导学生从无序数据整理的实际需求出发，经历“物理模拟 ➔ 逻辑抽象 ➔ 流程图绘制 ➔ 程序编码 ➔ 效率分析”的计算思维全流程。
- **学情分析**：学生熟悉单层循环与列表访问，但对嵌套循环中循环变量的联动控制存在空间想象难度。

### 三、教学目标
1. **计算思维**：通过 5 位同学身高排队模拟，抽象出“相邻比较、大数沉底”的冒泡核心逻辑，能绘制算法流程图。
2. **数字化学习与创新**：利用 PRIMM 教学法，在脚手架代码支持下独立编写并调试双层 for 循环冒泡排序程序。
3. **信息社会责任**：体会算法优化在海量大数据检索与排序中的关键价值。

### 四、教学过程
1. **情境激趣 (5分钟)**：5 位同学手持随机数字卡片上台，由台下学生发指令排队，归纳出“气泡上浮”现象。
2. **算法解构 (10分钟)**：推导 $n$ 个数需要进行 $n-1$ 趟排序，第 $i$ 趟只需比较 $n-1-i$ 次。
3. **PRIMM 编程探究 (20分钟)**：
   - Predict: 预测单趟比较代码的执行结果。
   - Modify: 将单趟代码封装入外层循环。
   - Make: 补充提前结束冒泡的布尔标记 `flag` 进行算法优化。
4. **展示与总结 (10分钟)**：对比 1 万条乱序数据在普通冒泡与优化冒泡下的比较次数，完成任务单自我评价。

---

## 4. 生成产物二：《冒泡排序算法探究》课堂学习任务单

**班级**：高一 (3) 班 **姓名**：_______________ **学号**：_______________

### 🎯 学习目标
- [ ] 掌握冒泡排序“两两比较、交换下沉”的基本原理。
- [ ] 补全并运行双层嵌套循环的 Python 冒泡排序代码。

### 💡 探究任务：代码填空与调试
请阅读以下半成品代码，在横线处补全外层循环与内层循环的范围控制：

```python
# 待排序测试数据
scores = [68, 89, 45, 92, 77]
n = len(scores)

# 外层循环控制“排序趟数”
for i in range(________):  # 提示：共需要 n-1 趟
    # 内层循环控制“本趟两两比较次数”
    for j in range(________):  # 提示：每趟减少比较 i 次
        if scores[j] > scores[j + 1]:
            # 相邻元素交换
            scores[j], scores[j + 1] = scores[j + 1], scores[j]

print("排序后的结果：", scores)
```

### 🚀 挑战拓展：设置 `flag` 提前终止
如果某趟遍历中一次交换都没有发生，说明列表已经有序，应该如何使用 `flag = False` 实现提前 `break`？
